
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    # nginx.ingress.kubernetes.io/rewrite-target: /$2
  name: tomato
  namespace: tomato-ns
spec:
  rules:
  - host: 35.197.128.239.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tomato
            port:
              number: 8765